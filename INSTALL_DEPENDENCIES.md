# Frontend dependencies to install
cd frontend
npm install react-webcam@7.1.1

# Backend Python dependencies
cd backend
pip install opencv-python numpy

# Optional: If dlib is needed for better face detection
pip install dlib

# Or use opencv-contrib-python which includes dlib features
pip install opencv-contrib-python
